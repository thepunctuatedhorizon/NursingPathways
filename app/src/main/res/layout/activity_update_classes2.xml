<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/bg"
    tools:context=".UpdateClassesInProgress">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:id="@+id/scrollViewUpdateClassesReal">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:id="@+id/relativeLayoutUpdateReal"
            android:gravity="center_horizontal">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Please select the courses you have successfully completed or that your adviser has said will transfer.  It is ok if you haven&apos;t taken all of the classes listed."
                android:id="@+id/pleaseUpdateReal"
                android:gravity="center_horizontal"
                android:background="@android:drawable/btn_default"
                android:textColor="@color/abc_primary_text_disable_only_material_light" />


        </LinearLayout>
    </ScrollView>
</RelativeLayout>

